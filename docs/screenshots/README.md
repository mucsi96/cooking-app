# Recipe thumbnail UI screenshots

Captured with Playwright against the local cooking-app test stack, using mocked
AI image generation and test recipe data.

- `recipe-card-spacing-desktop.png`: recipe list card content spacing (1280 × 900).
- `thumbnail-dialog-desktop.png`: thumbnail generation and selection dialog (1280 × 900).
- `thumbnail-dialog-mobile.png`: the same dialog at a mobile viewport (375 × 812).

The colored candidate images are the image-generation mock's output.
